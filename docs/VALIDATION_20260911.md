# Numerical and reproduction audit — September 11, 2026

The interrupted revision was resumed from commit `2edff8b`; existing simulations were retained. No arithmetic discrepancy was found in the main or budget summaries.

## Reproduction and numerical checks

- A fresh `git archive HEAD` extraction, with no build or cache directory, successfully ran `python reproduce.py` from outside the repository. All pre-existing generated LaTeX tables, `numbers.tex`, summary CSVs and paired-contrast CSVs matched the committed outputs byte for byte.
- The main results contain exactly 29,400 records: 49 method/schedule records for each of 600 generated datasets, with the specified 200 seeds in each of three designs. The 147 summary rows and their MCSEs agree with independent aggregation of the raw records.
- Support metrics were recomputed directly from TP, FP, FN and TN. All 219 empty selected models remain in the summaries and have precision, recall, F1 and Jaccard equal to zero.
- FedAvg-P costs equal the sum of its five recorded candidate round counts. Selected-fit and discarded-candidate costs, scalar validation costs, total KiB and total exchanges satisfy their stated identities. Unmeasured ADMM-CV communication remains missing.
- The budget study contains 4,650 returned-fit records, 6,750 rate-candidate records and 150 shared-run ledger records. Every reported DA fit matches the validation-MSE winner among its three candidates, including the ascending-rate tie rule. Objective gaps agree with subtraction of the paired pooled reference. Every pooled reference has a proximal-gradient mapping residual below `1e-7`.
- All 93 budget summary rows, existing paired contrasts and their MCSEs match independent calculations. Standalone and reused-prefix communication costs agree with their stated byte formulas.

The five core numerical tests, six FedDualAvg identity/convergence tests and seven committed-result audit tests pass:

```bash
python -m unittest discover -s ms/code/tests -v
python -m unittest discover -s tests -v
```

## Added total-cost analysis

The new total-upload-cap comparison is a deterministic reanalysis of the archived budget records. Its rule chooses the largest recorded checkpoint affordable under 1,440,120 bytes, counting all candidate paths and setup/validation scalars. This selects CD100 and DA20. The new supplementary table, summary CSV and paired-contrast CSV are regenerated by `budget_study.py --from-results` and `reproduce.py`.

[BUDGET_TOTAL_CAP.md](BUDGET_TOTAL_CAP.md) reports the actual spending and paired contrasts. Actual spending differs because the checkpoint grid is coarse; no interpolation or equal-spending claim is made. True-support F1 is higher and the objective gap is lower for DA in all nine design/local-step combinations. The correlated-design, E=1 test-MSE contrast is small relative to its MCSE, so uniformly decisive predictive improvement is not claimed. ST and P were not run in this comparison.

## Source and provenance

The build now packages only the local source files, figures and bibliography transitively referenced by `main.tex` and `supplement.tex`. Unreferenced archival diabetes material remains in the repository and is excluded from the article source archive. The abstract length and manuscript word-count checks remain active; the build does not print a displayed manuscript word-count statement.

Changes to `budget_study.py` add rendering and aggregation of the total-cap sensitivity analysis. Its simulation functions and all original raw draws are unchanged. The source hashes in the original run metadata remain historical records of the earlier delivered source; the current render-only extension is documented here. The archived diabetes table generator no longer inserts an AI-assistance sentence into its table caption.

Original result files verified unchanged from the starting commit:

| File | SHA-256 |
|---|---|
| `sim_all_raw.csv` | `7f76e98eb6a85fcdb0a237df0eb4e893c55270a31068376df6f78bb4c5bebf6a` |
| `budget_raw.csv` | `9ebf4adb4a1c0850bea5a674591e129cd40f6c8852e55fa9981a744ef3ce4565` |
| `budget_candidates.csv` | `66780e44ea2e71127e0eb56698cd007c76531bfd1bd6b1be20f4ff543f7f5699` |
| `budget_cost_ledger.csv` | `4450355ea093b6f61ba3c5aad9a842a43b49eff01c09d70e4489993e8c1cc2a5` |
| `real_data_results.csv` | `7677f49e10925b0699f309bbbde7b73ae0f80c048d7347a98a0c16084e8da4f7` |

## Final package checks

- The final manuscript is 22 pages and Supplement S1 is 4 pages. All pages were rendered and inspected; the changed budget table and supplemental table were also inspected at reading size. No clipping, missing references or overfull boxes remained.
- The abstract contains exactly 150 whitespace-delimited words. The build records an approximate inclusive manuscript length of 9,692 words in `ms/word_count.tex`, without displaying it on the title page.
- The 27-file LaTeX source archive was extracted into a new directory and both documents compiled successfully. Rebuilt PDFs matched the delivered PDFs in page count, extracted text and rendered pixels on every page.
- The final PDFs contain the requested Supplement S1 reference and exclude the removed diabetes illustration, AI-production caption notes and visible word-count statement.
